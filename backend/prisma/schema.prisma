generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model hello {
  id Int        @id @default(autoincrement())
  magic_word    String
}
